import QuadTree from '../../../../../../util/quad-tree';

interface StrokeSizeState {
  quadTree?: QuadTree;
}

export const DEFAULT_STROKE_SIZE_STATE: StrokeSizeState = {};

export default StrokeSizeState;
